# DingBot

> 🚧 WIP

```go
// 设置 access_token 与 secret
dingbot.Token("your token")
dingbot.Secret("your secret")

// 发送各类消息
dingbot.Text(...)
dingbot.Link(...)
dingbot.Markdown(...)
dingbot.Action(...)
dingbot.Feed(...)

// 暂未实现令牌桶
```